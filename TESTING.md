# คู่มือทดสอบ LenShare — บริหารเวลาให้จบวงใน 30 นาที

> เป้าหมาย: ทดสอบ end-to-end flow 1 วงเต็ม (สร้าง → เชิญ → ประมูลทุกงวด → ปิดวง) ครบทุก feature สำคัญ
> เวลารวม: **~30 นาที** (3 บัญชี, 3 งวด)

---

## ⏱ ตารางเวลารวม

| Phase | เวลา | คนทำ | ผลลัพธ์ |
|---|---|---|---|
| 0. เตรียม env | 5 นาที | คนเดียว | 3 บัญชี + วง 1 วง |
| 1. งวดที่ 1 (ท้าวรับอัตโนมัติ) | 3 นาที | คนเดียว (A) | งวด 1 = "เสร็จสิ้น" |
| 2. งวดที่ 2 (tie-break B vs C) | 7 นาที | 3 คน | ทดสอบเสมอ + แก้เสมอ |
| 3. งวดที่ 3 (ปิดวง) | 3 นาที | 2 คน | ปิดวงสมบูรณ์ |
| 4. ตรวจสอบสรุป | 3 นาที | คนเดียว | สุทธิทุกคนถูกต้อง |
| 5. cleanup / reset | 2 นาที | คนเดียว | พร้อมรอบใหม่ |

---

## 🔧 Phase 0 — เตรียม environment (5 นาที)

### 0.1 ตรวจ pre-requisites (1 นาที)
- [ ] dev server รันอยู่ที่ `http://localhost:3000`
- [ ] `.env.local` มี Supabase URL/keys + `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`
- [ ] รัน `supabase/schema.sql` + `supabase/add_location_fields.sql` แล้ว
- [ ] Google Cloud: เปิด Maps JS API + Places API + ผูก billing แล้ว

### 0.2 ปิด email confirmation (1 นาที, ครั้งเดียว)
Supabase Dashboard → **Authentication → Providers → Email** → ปิด `Confirm email` → Save

### 0.3 สร้าง 3 บัญชีทดสอบ (3 นาที)
เปิด 3 browser context พร้อมกัน:

| Slot | Browser | Email | บทบาท |
|---|---|---|---|
| 🟦 A | Chrome (default) | `you+a@gmail.com` | ท้าวแชร์ |
| 🟩 B | Chrome Incognito | `you+b@gmail.com` | ลูกแชร์ #1 |
| 🟧 C | Edge หรือ Firefox | `you+c@gmail.com` | ลูกแชร์ #2 |

ทั้ง 3 → `/signup` → ใส่ email + รหัสผ่าน + display name → เข้าสู่ระบบ
> 💡 ใช้รหัสเดียวกันทุกบัญชีเพื่อจำง่าย เช่น `Test1234!`

---

## 🎯 Phase 1 — งวดที่ 1 (5 นาที)

### 1.1 A สร้างวง (1 นาที)
A → `/groups/new` กรอก:
```
ชื่อวง:        ทดสอบ-{วันที่}
ประเภท:        ดอกหัก
เงินสมาชิก/งวด: 1000
เงินท้าว/งวด:   1000
ค่าท้าว:       0
ท้าวประมูลร่วม: ☐ (ไม่ต้องติ๊ก — ท้าวรับงวด 1 อัตโนมัติอยู่แล้ว)
step:          10
ระยะเวลาประมูล: 1 ชั่วโมง
จำนวนงวด:      3
ความถี่:       รายสัปดาห์
วันเริ่ม:      วันนี้
```
→ กดสร้าง → จะอยู่ที่ `/groups/{id}` พร้อม invite code

### 1.2 B และ C เข้าร่วม (1 นาที)
A → คัดลอกลิงก์เชิญ → ส่งให้ B, C ผ่าน clipboard / Discord / LINE
B และ C → เปิดลิงก์ → กรอก display name → "เข้าร่วม"
A → refresh → ต้องเห็น 3 คน (รวมท้าว)

### 1.3 งวด 1 — ท้าวรับเงินก้อนอัตโนมัติ (1 นาที)

> ⚠ กฎใหม่: งวด 1 ของทุกวง = ท้าวแชร์รับอัตโนมัติ ไม่มีการประมูล (ไม่มีดอก)

| เวลา | คน | การกระทำ |
|---|---|---|
| 0:00 | A | กางงวด 1 → เห็น banner "🎯 งวดที่ 1 — ท้าวแชร์รับเงินก้อนอัตโนมัติ" |
| 0:15 | A | แก้ไขข้อมูลรอบ → ค้น "MK สุกี้" → เลือก → meal: ท้าวจ่าย → บันทึก |
| 0:30 | A | กด **"ปิดงวด (ท้าวรับเงินก้อน)"** → ท้าวเป็น winner ทันที, ดอก = 0 |
| 0:45 | A | ติ๊กสถานะจ่ายของ B และ C → "ปิดงวด (เสร็จสิ้น)" |

**ตรวจสอบ:**
- [ ] งวด 1 status = "เสร็จสิ้น", winner = ท้าว, ดอก = 0
- [ ] ไม่มี bidding form ในงวด 1 ทั้งฝั่ง A, B, C
- [ ] คลิก 📍 MK สุกี้ → เปิด Google Maps แท็บใหม่ได้
- [ ] B และ C เห็นข้อมูลเดียวกันหลัง refresh
- [ ] ในงวด 2+ ท้าวถูกกันออกจาก eligible bidders (เพราะเคยเปียแล้ว)

---

## 🔁 Phase 2 — งวดที่ 2: sealed-bid + tie-break อัตโนมัติ 10 นาที (7 นาที)

> A เป็นท้าวเปียงวด 1 ไปแล้ว → งวด 2 เปียได้แค่ B, C
>
> 🔒 **Sealed bid**: ระหว่างเปิดประมูล ไม่มีใคร (รวมท้าว) เห็นจำนวนดอกของผู้อื่น เห็นแค่ "✓ ส่งแล้ว / ⏳ รอ"

| เวลา | คน | การกระทำ |
|---|---|---|
| 0:00 | A | กางงวด 2 → "เปิดประมูล" |
| 0:30 | A,B,C | refresh → ทุกคนเห็นสถานะ "0/2 ส่งดอกแล้ว" + ⏱ countdown |
| 1:00 | B | ใส่ดอก `200` → ส่ง → A,C เห็นแค่ "B ✓ ส่งแล้ว" (ไม่เห็นจำนวน) |
| 1:30 | C | ใส่ดอก `200` → ส่ง → A,B เห็น "C ✓ ส่งแล้ว" |
| 2:00 | A | refresh → กด "ปิดประมูล" |
| 2:01 | ระบบ | ตรวจพบเสมอ → auto เริ่ม **tiebreak iteration 1** + ตั้ง countdown 10 นาที |
| 2:30 | B,C | refresh → เห็นสถานะ "🔒 แก้เสมอครั้งที่ 1" + ⏱ countdown 9:XX |
| 3:00 | B | ใส่ดอก `210` → ส่ง |
| 3:30 | C | ใส่ดอก `220` → ส่ง |
| 4:00 | A | "ปิดประมูล" → C ชนะ ($220) |
| 4:30 | A,B,C | เห็น "📊 สรุปการประมูล (เปิดเผย)" — โชว์ทุก iteration ทุกคน + 🏆 ผู้ชนะ |
| 5:00 | A | ติ๊กสถานะจ่าย A และ B → "ปิดงวด (เสร็จสิ้น)" |

**ตรวจสอบ:**
- [ ] A (ท้าว) ไม่อยู่ใน eligible bidders
- [ ] ระหว่างเปิดประมูล ไม่มีใครเห็นจำนวนดอกคนอื่น (รวมท้าว) — เห็นแค่ ✓/⏳
- [ ] กด "ปิดประมูล" ตอนเสมอ → ระบบเข้าสู่ tiebreak อัตโนมัติ ไม่มี error
- [ ] ⏱ Countdown แสดง MM:SS ทุกวินาที, สีส้ม < 1 นาที, สีแดงเมื่อหมดเวลา
- [ ] เฉพาะ B, C เปียได้ใน tiebreak (A กดส่งดอกไม่ได้)
- [ ] หลังปิดประมูลรอบสุดท้าย → BidRevealSection โชว์ทุก iteration พร้อม 🏆

---

## 🏁 Phase 3 — งวดที่ 3: ปิดวง (3 นาที)

เหลือ B คนเดียวที่ยังไม่ชนะ:
| เวลา | คน | การกระทำ |
|---|---|---|
| 0:00 | A | กางงวด 3 → "เปิดประมูล" |
| 0:30 | B | ใส่ดอก `10` (ขั้นต่ำ — ไม่มีคู่แข่ง) → ส่ง |
| 1:00 | A | "ปิดประมูล" → B ชนะ |
| 1:30 | A | ติ๊กสถานะจ่าย A และ C → "ปิดงวด (เสร็จสิ้น)" |

**ตรวจสอบ:**
- [ ] B เป็นคน eligible เพียงคนเดียวในงวดนี้
- [ ] หลังปิดงวด → ทุกคน (A, B, C) มี `wonRound` ครบ

---

## 📊 Phase 4 — ตรวจสอบสรุปยอด (3 นาที)

A เลื่อนลงล่างหน้าวง → ดูตาราง "สรุปยอดต่อคน":

**สิ่งที่ต้องเช็ค:**
- [ ] **ยอดสุทธิรวมของทุกคน ≈ 0** (เงินไม่หายจากระบบ — ผลรวม `net` ใกล้ศูนย์)
- [ ] ทั้ง 3 คนมีคอลัมน์ "เปียงวด X" ครบ — ไม่มี "ยังไม่เปีย"
- [ ] จำนวน "จ่ายไป" ของ B = `1000 × 3 - ดอกที่ B จ่าย` (ผู้ที่ยังไม่เปียจ่ายน้อยกว่า)
- [ ] "ได้รับ" ของผู้ชนะแต่ละงวด = เงินก้อนรวม − ดอก − ค่าท้าว

ลอง B และ C ก็ดูตารางเดียวกันได้ — ตัวเลขต้องตรงกัน (ทดสอบ RLS)

---

## 🧹 Phase 5 — Reset สำหรับรอบทดสอบใหม่ (2 นาที)

### Quick reset เฉพาะวงทดสอบ
Supabase Dashboard → **SQL Editor** → New query:
```sql
delete from public.share_groups where name like 'ทดสอบ-%';
```
> cascade ลบ memberships, rounds, bids, payment_status ให้อัตโนมัติ

### Full reset (เริ่มใหม่หมด)
```sql
delete from public.share_groups;
delete from auth.users where email like 'you+%@gmail.com';
```

---

## 🚨 Troubleshooting ระหว่างทดสอบ

| ปัญหา | สาเหตุ / วิธีแก้ |
|---|---|
| `ApiNotActivatedMapError` | เปิด Maps JS API + Places API ใน GCP |
| Hydration mismatch | ปิด browser extension (uBlock, Grammarly) |
| B/C ไม่เห็นข้อมูลใหม่ | กด refresh ในแท็บนั้น (ไม่มี real-time push) |
| "มีผู้ประมูลเสมอ" ตลอด | ต้องกด "ประมูลแก้เสมอ" ก่อน ไม่ใช่ "ปิดประมูล" |
| ปิดประมูลแล้วผู้ชนะหาย | ตรวจ RLS — owner_id ต้องตรง user.id ของ A |
| Login วน redirect | ปิด `Confirm email` ใน Supabase Auth settings |

---

## 🧪 Smoke test (60 วินาที)

ทดสอบเร็วก่อน deploy ทุกครั้ง:
1. A สมัคร → สร้างวง 2 งวด, ลูกแชร์ = ท้าวเอง — ใช้ได้ไหม?
2. ค้นร้านใน Google Maps → autocomplete ขึ้นไหม?
3. กด 📍 ในรายการรอบ → เปิด Maps ได้ไหม?
4. Refresh หน้า → ไม่มี hydration error?

ถ้า 4 ข้อนี้ผ่าน → flow ปกติน่าจะใช้งานได้

---

## 📈 Coverage matrix

| Feature | งวด 1 | งวด 2 | งวด 3 |
|---|---|---|---|
| สร้างวง / invite code | ✅ | | |
| Multi-user join | ✅ | | |
| Google Places picker | ✅ | | |
| **ท้าวรับงวด 1 อัตโนมัติ** | ✅ | | |
| Bid → close → winner | | ✅ | ✅ |
| Tie-break flow | | ✅ | |
| Past winner exclusion (ท้าวออก) | | ✅ | ✅ |
| Single-bidder rounds | | | ✅ |
| Payment checklist | ✅ | ✅ | ✅ |
| Round complete | ✅ | ✅ | ✅ |
| สรุปยอดสุทธิ | | | ✅ (after) |
| RLS (B/C อ่านได้, แก้ไม่ได้) | ✅ | | |
